/*properties
    insert
*/

function webEq(soundSrc, options) {
		options = options || new Options();
		options.insert('sound-source', soundSrc);
}


var Options = Options || (function() {
			// here will all Options be set as by default they are to be set
			// but for now I have no clue what would go here		
}
